private YOUR_FORMATTER() {
...
    document = `
    
        // Creazione del codice HTML contenente i dati  
    
    `
...
    let template = Handlebars.compile(document);    
    return template({var: parsed_command_VAR});
}